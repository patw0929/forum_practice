<h1>建立新版塊</h1>

<%= form_for(@board) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>

  <%= f.label :name, "Board name" %><br />
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>

<%= link_to("返回", boards_path) %>