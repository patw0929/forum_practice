<h1>新增文章</h1>

<%= form_for @post, :url => board_posts_path(@board, @post), :html => { :multipart => true } do |f| %>
  <%= render "form", :f => f %>
<% end %>

<%= link_to("返回", board_posts_path(@board)) %>